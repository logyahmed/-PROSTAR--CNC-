# فك الضغط
unzip prostar-site.zip
cd prostar-site

# ربط بـ GitHub
git init
git add .
git commit -m "Initial: Prostar CNC website"
git branch -M main
git remote add origin https://github.com/USERNAME/prostar-cnc.git
git push -u origin main
